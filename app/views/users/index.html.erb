<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <li>
    <% @users.each do |user| %>
        <li>
        <%= link_to user.name, user %>
        <%= user.email %>
        </li>
    <% end %>
  </li>
</ul>
